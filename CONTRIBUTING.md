# 参与开发

欢迎修 bug、补测试、改文档，也欢迎指出与冻结 Java 基线不同的行为。项目正在推进芋道 Cloud 后端的 Go 重构，当前版本仍需兼容性验证；请把问题说到能复现。

## 提问题

普通 bug 请写明 Go commit、运行方式、请求/输入、预期、实际结果，以及能复现的最小步骤。涉及 Java 差异时附固定 Java SHA 的对应接口或测试。日志请先删掉令牌、密码、手机号等真实数据。安全漏洞不要发公开 Issue，按 [SECURITY.md](SECURITY.md) 走私密渠道。

## 提交改动

1. 先读[开发说明](docs/development/index.md)和[架构说明](docs/development/architecture.md)，选择一个可单独验证的小功能。
2. 改代码时让 handler 处理 HTTP、用例处理规则、适配器处理外部依赖。跨 Java/Go 的特殊规则写清楚中文注释。
3. 为新增行为选择有区分力的测试；必要时跑 MySQL/Redis/Nacos 集成测试。改兼容性行为时提供 Java 或前端对照证据。
4. 在 PR 描述中写明本次目的、改动、命令及结果、尚未覆盖的风险；需要时同步更新使用说明、开发说明或重构过程。每个 PR 应能独立看懂，不依赖维护者的本机记录。

提交消息格式是 `type(scope): 具体技术动作`，例如 `feat(auth): 实现短信验证码原子消费`、`fix(file): 校验下载文件路径`、`docs(architecture): 解释依赖方向`。标题写可观察的变化，正文写为什么改、怎么验证；避免“补齐接口”“优化代码”这类没有范围的标题。请不要提交 `.env`、密钥、数据库导出或个人路径。项目目前不会自动部署 Go 服务，合并与发布由维护者审核。

想从头跟做一遍，可从[重构过程](docs/refactor/index.md)开始；章节的例子和实际实现有出入时，请开 Issue 指明文件与版本。
